server:
  tomcat:
    uri-encoding: UTF-8
    max-http-form-post-size: 2MB
spring:
  application:
    name: cloud-auth
  # 环境 dev|test|prod
  profiles:
    active: dev
  
    
#mybatis
mybatis-plus:
  mapper-locations: classpath:map/**/*.xml
  #实体扫描，多个package用逗号或者分号分隔
  typeAliasesPackage: cn.sinvie.oauthserver.Entity
  global-config:
    #数据库相关配置
    db-config:
      #主键类型  AUTO:"数据库ID自增", INPUT:"用户输入ID", ID_WORKER:"全局唯一ID (数字类型唯一ID)", UUID:"全局唯一ID UUID";
      id-type: AUTO
      #字段策略 IGNORED:"忽略判断",NOT_NULL:"非 NULL 判断"),NOT_EMPTY:"非空判断"
      field-strategy: NOT_NULL
      #驼峰下划线转换
      column-underline: true
      logic-delete-value: 1 # 逻辑已删除值(默认为 1)
      logic-not-delete-value: 0 #逻辑未删除值(默认为 0)
      banner: false
  #原生配置
  configuration:
    map-underscore-to-camel-case: true
    cache-enabled: false
    call-setters-on-nulls: true
    jdbc-type-for-null: 'null'
security:
  oauth2:
    client:
#      user-authorization-uri: http://localhost:8083/oauth/token
#      client-id: c1
#      resource-ids: res1
#      client-secret: secret
#      scope: all
       grant-type: password
#      pre-established-redirect-uri: https://www.baidu.com/
#      authorities:
#      access-token-validity-seconds: 7200
#      refresh-token-validity-seconds: 259200
#      additional-information:
#      auto-approve-scopes: false